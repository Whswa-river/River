# RiverBox 插件开发记录

## 项目信息
- **插件名称**: RiverBox
- **DLL路径**: `D:\zjc\lua\box\RiverBox\bin\Debug\RiverBox.dll`
- **Dalamud API**: 15, .NET 10.0
- **UI风格**: 赛博朋克（霓虹青/紫/粉、深紫黑背景）

---

## 功能模块

### 1. 自动收艇 (SubmarineCollect)
- 勾选角色后点击"开始收艇"，逐个切换角色并调用 DailyRoutines 收艇
- 返航检查：点击开始时检测缓存，无已返航潜艇则提示"未有任何潜水艇返航"
- 回调逻辑：检测 SelectString 窗口状态，关闭后执行下一个
- 单角色收艇：独立按钮，直接调用 `/pdr submarine`
- 每个角色显示：名称、4艘潜艇表格（艇名/等级/返航时间/状态）、最后更新时间
- 操作按钮：[删除] [切换] [DR收艇]
- 进度显示：`(1/3)` 格式显示当前角色进度

### 2. 宏快捷发送 (MacroQuickSend)
- 赛博风悬浮窗（`/RB mqs` 打开）
- 配置面板在"日常"标签页
- 分组管理：添加/删除/重命名分组
- 宏管理：添加/编辑/删除/排序宏
- 导入导出：JSON格式，直接读写剪贴板（Windows API）
- 启用模块检查：未启用时悬浮窗无法打开

### 3. 插件管理 (PluginManager)
- 分组功能：创建/删除/重命名分组
- 插件操作：启用/禁用/置顶/重命名/移动到分组
- 批量操作：[移出全部] [启用全部] [禁用全部] [重命名]
- 搜索框：支持按名称和内部名称模糊搜索
- 置顶功能：左键置顶，右键取消置顶

### 4. 自动跟随 (AutoFollow)
- 选中目标或指定名称跟随
- 超出距离自动 `/follow`，进入范围停止
- 战斗中暂停选项
- 距离回滞：30% 缓冲区避免抖动
- 丢失目标重试：每 300 帧（约 5 秒）重新尝试跟随
- 赛博风悬浮窗：仅显示"取消跟随 XX"按钮（`/RB af` 打开）

### 5. 其他功能
- 卡死检测 (IdleNotify)
- 原地复活 (AutoRevive)
- 隐藏怪物 (BlockEnemies)

---

## 命令

| 命令 | 功能 |
|------|------|
| `/riverbox` | 打开主界面 |
| `/RB` | 打开主界面 |
| `/RB mqs` | 打开宏快捷发送悬浮窗 |
| `/RB af` | 跟随选中目标 |
| `/RB af 角色名` | 跟随指定角色 |

---

## 最近改动

### 2026-08-22
1. **自动收艇布局调整** - 开始按钮移到顶部，角色列表在中间，添加预设在底部
2. **切换角色修复** - `Svc.Chat.Print` 改为 `Chat.ExecuteCommand` 执行命令
3. **添加 CallbackCommand** - 新增加载按钮
4. **回调逻辑优化** - 从固定延迟改为检测 SelectString 窗口状态
5. **潜艇返航检查** - 无返航潜艇时提示并阻断
6. **宏快捷发送重构** - 赛博风悬浮窗，配置移入日常标签页
7. **搜索功能删除** - 移除主窗口搜索框
8. **命令简化** - Dalamud 列表只显示 `/riverbox`，`/RB` 仍可用
9. **ON/OFF按钮删除** - 移除无功能的开关按钮
10. **导入导出修复** - 改为剪贴板方式，JSON格式，修复空导出问题
11. **插件管理增强** - 新增启用全部/禁用全部/重命名分组/搜索框
12. **自动跟随修复** - 补全跟随移动逻辑
13. **自动跟随丢失目标重试** - 每 300 帧重新尝试跟随一次
14. **自动跟随悬浮窗精简** - 仅显示取消按钮，移除"跟随: XX"文字

---

## 技术要点

- `Chat.ExecuteCommand()` 来自 `ECommons.Automation`，用于执行聊天命令
- `Svc.GameGui.GetAddonByName()` 返回 `AtkUnitBasePtr`，用于检测UI窗口
- `System.Text.Json` 默认只序列化属性（`{ get; set; }`），不序列化字段
- Windows剪贴板API通过 P/Invoke 调用 `user32.dll` 和 `kernel32.dll`
- ImGui 重入保护：`_drawing` 标志防止递归绘制崩溃

---

## 文件结构

```
RiverBox/
├── RiverBox.cs           # 插件入口，命令注册
├── UIWindow.cs           # 主界面UI，赛博风主题
├── SubmarineCollect.cs   # 自动收艇逻辑
├── SubmarineConfig.cs    # 收艇配置和缓存
├── SubmarineStatus.cs    # 潜艇状态读取
├── MacroQuickSend.cs     # 宏快捷发送
├── PluginManager.cs      # 插件管理
├── AutoFollow.cs         # 自动跟随
├── AutoRevive.cs         # 原地复活
├── IdleNotify.cs         # 卡死检测
├── BlockEnemies.cs       # 隐藏怪物
└── Config.cs             # 配置类
```
